spring:
  application:
    name: lavajato
  datasource:
    url: jdbc:mysql://localhost:3306/LavaRapido?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  thymeleaf:
    cache: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: silvajasiel30@gmail.com
    password: uatakfluohmgczdo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
  error:
    include-message: always
    include-binding-errors: always
